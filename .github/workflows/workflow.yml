name: mi primer workflow
 
on: [push]
 
jobs:
    build:
        runs-on: ubuntu-latest
 
        steps:
            - name: echo a string
              run: |
                    echo "Hello World"
                    echo "chau world"
                    pwd